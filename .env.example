# Required — never expose this to the client. Used only inside src/app/api/transform/route.ts
GEMINI_API_KEY=AIza...

# Optional — override the model used for study-flow generation
# Model: gemini-1.5-flash (recommended & default), gemini-2.0-flash, gemini-1.5-pro
GEMINI_MODEL=gemini-1.5-flash
